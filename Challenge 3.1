def
linear searchproduct(product list,target product):
indices=
for index,product in enumerate(product list):
if product==target product:
indices append(index)
return indices


 # Example usage:
 products=["shoes","boot","loafer","shoes","sandal","shoes"]
 target="shoes"
 target="apple"
 result=linear search product(products,target)
 print(result)
